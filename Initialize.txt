https://feyziyev007.medium.com/how-to-install-openmpi-on-ubuntu-18-04-cluster-2fb3f03bdf61

$ sudo apt install build-essential
$ sudo apt install gcc

Install mysql-client:
$ sudo apt-get install mysql-client
$ sudo apt-get install libmysqlclient-dev

open XAMPP:
$ sudo /opt/lampp/lampp start

Configure MPI:
$ sudo apt-get install flex
$ sudo apt-get install pandoc

$ git clone https://github.com/AnnaLena77/ompi-project.git --recurse-submodules

Needed packages for autogen:
$ sudo apt install autoconf
$ sudo apt install libtool

$ ./autogen.pl
//Cofigure with Analysis, if you want to use the original Open_MPI, don't use the CFLAGS-Parameter!
$./configure --prefix=/home/anna-lena/sharefolder/mpi LIBS=-lmysqlclient --with-pmix=internal CFLAGS=-DENABLE_ANALYSIS=1



Make MPI:
$ sudo make all install

$ sudo ldconfig

Compile and run MPI-Program:
$ mpicc mpitest.c -o mpitest
$ mpirun -n 2 --oversubscribe ./mpitest

mpirun -np 24 --hostfile /home/anna-lena/sharefolder/my_hosts ./osu_allreduce




